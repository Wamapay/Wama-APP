{
  "name": "learn-and-earn-admin-dashboard",
  "version": "1.0.0",
  "description": "Admin Dashboard for the Learn & Earn platform — bundles to a single static index.html (React + Tailwind, no runtime CDN dependency).",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i tailwind-input.css -o tailwind-output.css --minify",
    "build:js": "esbuild src/entry.jsx --bundle --minify --jsx=automatic --loader:.js=jsx --outfile=bundle.js",
    "build": "npm run build:css && npm run build:js && node build.js",
    "test": "echo \"No automated tests for the admin dashboard yet\" && exit 0"
  },
  "dependencies": {
    "esbuild": "^0.28.2",
    "lucide-react": "^1.34.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
