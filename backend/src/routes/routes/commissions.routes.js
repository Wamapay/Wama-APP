"use strict";

/**
 * commissions routes -- architecture placeholder.
 * Full implementation arrives in Backend Stage 6 (Finance).
 */

const buildPlaceholderRouter = require("../utils/placeholderRouter");

module.exports = buildPlaceholderRouter("commissions", "Backend Stage 6 (Finance)");
