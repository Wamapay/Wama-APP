"use strict";

/**
 * notifications routes -- architecture placeholder.
 * Full implementation arrives in Backend Stage 7 (Notifications).
 */

const buildPlaceholderRouter = require("../utils/placeholderRouter");

module.exports = buildPlaceholderRouter("notifications", "Backend Stage 7 (Notifications)");
