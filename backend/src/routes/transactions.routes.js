"use strict";

/**
 * transactions routes -- architecture placeholder.
 * Full implementation arrives in Backend Stage 6 (Finance).
 */

const buildPlaceholderRouter = require("../utils/placeholderRouter");

module.exports = buildPlaceholderRouter("transactions", "Backend Stage 6 (Finance)");
